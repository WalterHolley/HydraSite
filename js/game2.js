const Game = () => {

  const[userName, setUserName] = useState("CAP");
  const[enemyName, setENemyName] = userState("Hydra");
  const[enemyHP, setEnemyHP] = userState(0);
  const[playerHP, setPlayerHP] = userState(0);
}
